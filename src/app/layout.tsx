import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PhilTreeCrawler - A Living Map of Human Thought",
  description:
    "Traverse an ever-expanding graph of philosophical concepts. Each node provides lessons, and every branch you explore contributes to a shared knowledge base.",
  keywords: [
    "philosophy",
    "education",
    "learning",
    "concepts",
    "ethics",
    "metaphysics",
    "epistemology",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
